## RegexpToNFA

A Haskell project that implies a Glushkov's construction algorithm

## How to run the project

Use cabal to launch the project:

`cabal build`
`cabal run RegexpToNFA` 

## AI assistants usage

This project was made with some usage of AI assistance (DeepSeek). AI used for these purposes:

- Which libraries use, which functions I need and what for
- Writing LaTeX code, writing function that draw DFA in a circle
- Error exceptions
- Some questions like "How to do this in Haskell"